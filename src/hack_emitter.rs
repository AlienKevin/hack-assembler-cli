use crate::hack_parser::*;

// pub fn emit(instructions: Vec<Instruction>) -> String {
//   instructions.iter()
//     .map(|instruction|
//       match instruction {
//         Instruction::A(number) =>

//       }
//     )
// }